/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    // assetPrefix: '/pipedemo/',
    experimental: {
        // serverActions: true,
    },
}

module.exports = nextConfig
